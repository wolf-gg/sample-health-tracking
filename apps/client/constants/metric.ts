/**
 * These numbers are based from Healthy365 default values.
 * Current limitation is that these values are static.
 */
export const STEPS_TARGET = 5000
export const EXERCISE_MINUTES_TARGET = 30
export const SLEEP_HOURS_TARGET = 7
